import React from 'react';
const SectionTitle = ({ children }) => (
  <h2 style={{ fontFamily: 'var(--font-display)', fontSize: 18, marginBottom: 16 }}>{children}</h2>
);
export default SectionTitle;

